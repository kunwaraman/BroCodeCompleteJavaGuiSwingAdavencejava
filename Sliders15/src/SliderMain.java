public class SliderMain {
    public static void main(String[] args) {
        //jSlider = Gui component that lets user enter a value
        // by using adjustable sliding knob on a track
        SliderDemo sliderDemo = new SliderDemo();
    }
}
